# Production dependencies for MYNFINI Render deployment
# Core Flask web framework
Flask==2.3.2
Flask-CORS==4.0.0

# AI Provider APIs
anthropic>=0.28.0
openai>=1.0.0

# Security and Environment Management
python-dotenv>=1.0.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# System and Utility
colorama>=0.4.6
typing-extensions>=4.8.0
pyyaml>=6.0
cryptography>=45.0.0

# HTTP and Requests
requests>=2.32.0
urllib3>=2.0.0

# Production Server
gunicorn>=21.2.0  (for production WSGI server)

# Optional for debugging if needed
# rich>=13.0.0

# Core Python Standard Library Dependencies Only
# No need for additional heavy packages